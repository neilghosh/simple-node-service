apiVersion: v1
kind: Service
metadata:
  name: simple-node-app
  labels:
    run: simple-node-app
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    statefulset.kubernetes.io/pod-name: simple-node-app